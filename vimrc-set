" ---------- Scrolling ----------
"  To avoid slow scrolling when line is too long
set synmaxcol=512

" ---------- Bash ----------
"  Use bash as default shell
set shell=bash

" ---------- Swap files ----------
" Sets swap directory to .vim/swap
set backupdir=~/.vim/backup//
set directory=~/.vim/swap//
set undodir=~/.vim/undo//

" ---------- Airline ----------
"
"  Shows airline everytime
set laststatus=2
"
" Integrate with powerline fonts and icons
let g:airline_powerline_fonts = 1
let g:airline#extensions#tabline#enabled = 1

" Switch between / delete buffers
noremap <S-l> :bn<CR>
noremap <S-h> :bp<CR>
nmap <leader>d :bdelete<CR>
nmap <leader>D :bufdo bd<CR>

"---------- Rainbow Parentheses ---------
noremap <F7> :RainbowParenthesesToggle <CR>
au Syntax * RainbowParenthesesLoadRound
au Syntax * RainbowParenthesesLoadSquare
au Syntax * RainbowParenthesesLoadBraces

"Integration to system's clipboard
map <Leader>y "*y<CR>
map <Leader>p "*p<CR>
